The kasim runtime was installed into the existing namespace:

  {{ .Release.Namespace }}

This chart does not create, upgrade, or delete a Kubernetes cluster or its
namespace. The kasim CLI submits Scenarios separately and never invokes Helm.

Controller image version: {{ .Chart.AppVersion }}
Kubernetes compatibility: 1.30-1.36
Stable DRA v1: Kubernetes 1.34-1.36 only
KWOK: v0.8.0, image pinned by OCI digest

Observer and operator permissions are separate ClusterRoles:

  {{ include "kasim-runtime.fullname" . }}-observer
  {{ include "kasim-runtime.fullname" . }}-operator

CRDs are retained by Helm on uninstall. Uninstall removes only this release's
exact namespaced and cluster-scoped runtime objects; it does not delete
Scenario Instances, Synthetic Nodes, user workloads, or unrelated KWOK assets.
